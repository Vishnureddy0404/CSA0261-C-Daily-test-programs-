#include<stdio.h>
#include<math.h>
int main()
{
	int  n,sum;
	printf("enter a number:");
	scanf("%d",n);
	if(n<0,n,sum==0)
		printf("sum of first n natural numbers,n");
	else 
	printf("sum of not first natural numbers,sum");
	return 0;
	}
